name: Run ls command

on:
  push:
    branches:
      - develop_urp

jobs:
  run-ls:
    runs-on: self-hosted  # セルフホステッドランナーを指定
    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Run ls command
        run: ls
